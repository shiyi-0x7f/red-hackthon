# ============================================================
# 前端生产构建环境变量
#
# 用法：
#   1. 把本文件复制为 .env.production（Vite 构建时会自动读取）
#      cp .env.production.example .env.production
#   2. 按需填写下方字段
#   3. npm run build
#   4. 把 dist/ 目录交给部署环节（FastAPI StaticFiles / nginx / Cloudflare Pages / Vercel）
#
# Vite 约定：只有 VITE_ 前缀的变量才会被打进 bundle
# ============================================================

# ---- 后端地址（VITE_HTTP_BACKEND） ----
#
# **常见场景**：
#
# 1) 同域部署（推荐，最简单）
#    前端和后端由同一个 server / nginx / FastAPI 服务，使用相对路径：
#        VITE_HTTP_BACKEND=
#    或直接不设，让 services/index.ts 默认走 same-origin。
#    fetch('/api/v1/...') 会自动用当前页面的 origin。
#
# 2) 跨域部署（前端在 CDN，后端在独立服务器）
#    必须填完整地址：
#        VITE_HTTP_BACKEND=https://api.your-domain.com
#    同时 server 的 CORS_ORIGINS 要加上前端的部署域名。
#
# 3) 开发联调（前端 localhost:1420，后端 localhost:9100）
#    建议用 .env.local（本地私有）而非 .env.production：
#        VITE_HTTP_BACKEND=http://localhost:9100
#
VITE_HTTP_BACKEND=

# ---- 后端 API Key（VITE_HTTP_BACKEND_KEY） ----
#
# 对应 server 的 LEARNING_API_KEY 环境变量。
#
# **安全提醒**：
#   Vite 会把 VITE_ 变量打包进前端 bundle，客户端可以看到。
#   不要把真正的管理员 API Key 放这里，应该用一个"只读学习用"的 key。
#   如果希望完全隐藏 key，只能用方案 B / C（同源部署）+ 后端在服务端
#   自己校验 session，不走 Bearer token。
#
# 开发阶段可以留空（server 的默认 key 为 sk-ai-learning-change-me 时会放行）
VITE_HTTP_BACKEND_KEY=
